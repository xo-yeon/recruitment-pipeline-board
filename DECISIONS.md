# 설계 결정 기록

## 1. React + Vite 기반 SPA 선택

### 선택

React, Vite, TypeScript를 기반으로 애플리케이션을 구성하고 TanStack Query, CSS Modules, Vitest, React Testing Library를 사용한다.

### 이유

- 요구사항은 단일 채용 보드 화면이므로 SSR과 파일 기반 라우팅이 필요하지 않다고 가정했다.
- Vite는 최소 설정으로 개발 서버와 프로덕션 빌드를 구성할 수 있다.
- TypeScript로 지원자와 채용 단계 데이터 구조를 명확하게 제한할 수 있다.
- TanStack Query는 조회 캐시, mutation, 낙관적 업데이트와 롤백을 한 흐름으로 관리하기 적합하다.
- CSS Modules는 별도 UI 라이브러리 없이 컴포넌트별 스타일 범위를 분리할 수 있다.

## 2. 자체 mock API와 localStorage 저장 선택

### 선택

별도 서버나 MSW 대신 Promise 기반 mock API 계층을 직접 구현하고 성공한 단계 변경을 localStorage에 저장한다.

### 이유

- 200~800ms 네트워크 지연과 약 15% 변경 실패를 한 API 계층에서 명확하게 제어할 수 있다.
- 새로고침 후에도 단계 변경 결과를 유지할 수 있다.
- UI가 localStorage에 직접 접근하지 않아 저장 방식을 교체하기 쉽다.
- 추가 서버 실행 없이 `npm run dev`만으로 과제를 검토할 수 있다.

## 3. 드래그 앤 드롭 대신 액션 버튼 선택

### 선택

카드 내부의 명시적인 단계 이동 버튼으로 지원자 단계를 변경한다.

### 이유

- 별도의 드래그 앤 드롭 라이브러리 없이 구현할 수 있다.
- 버튼의 이름으로 이동 목적지를 명확하게 전달할 수 있다.
- Tab과 Enter 또는 Space만으로 단계 이동이 가능해 키보드 접근성을 자연스럽게 지원한다.
- 제한된 시간에 낙관적 업데이트, 실패 롤백, 저장과 경쟁 상태 검증에 집중할 수 있다.

## 4. 전체 캐시 스냅샷 롤백과 지원자별 요청 제어

### 선택

단계 이동 전에 전체 지원자 Query 캐시를 저장하고 UI를 즉시 변경한다. 실패하면 전체 이전 캐시를 복원하며, 같은 지원자의 요청이 진행 중일 때 추가 요청만 차단한다.

### 이유

- 네트워크 지연 중에도 카드가 즉시 이동해 낙관적 업데이트 요구사항을 충족한다.
- 전체 캐시를 복원하면 실패한 카드의 단계뿐 아니라 목록 순서까지 정확하게 되돌릴 수 있다.
- 지원자 ID별 요청 상태를 관리하면 같은 카드의 응답 순서가 뒤바뀌는 경쟁 상태를 막을 수 있다.
- 다른 지원자의 이동은 계속 허용해 보드 전체가 잠기지 않는다.
- 버튼 비활성화와 로딩 커서를 사용하지 않아 기존의 즉각적인 조작감을 유지한다.

## 5. 1,000건 고정 높이 가상 목록과 반응형 가로 스크롤 선택

### 선택

결정적인 지원자 시드 데이터를 1,000건으로 확장하고, 각 컬럼에서 현재 보이는 카드와 주변 카드만 렌더링하는 고정 높이 가상 목록을 사용한다.

### 이유

- 전체 카드 1,000개를 DOM에 동시에 생성하지 않아 스크롤과 필터 후 렌더링 부담을 줄일 수 있다.
- 기존 이름 검색과 직무 필터 결과는 `useMemo`로 계산해 불필요한 반복 계산을 줄인다.
- 카드 레이아웃이 일정하므로 외부 가상화 라이브러리 없이 고정 높이 방식으로 구현할 수 있다.
- 작은 화면에서는 카드 폭을 줄이지 않고 컬럼 너비를 유지한 채 보드에 가로 스크롤을 제공해 카드 UI가 깨지지 않도록 한다.

## 완료 여부

미완성 기능은 없다. 필수 요구사항과 이번 작업에서 선택한 도전 요구사항인 경쟁 상태 처리, 1,000건 가상 스크롤, Undo, 핵심 테스트, 키보드 접근성을 모두 완료했다.

---

# 커밋별 작업 요약

기존 커밋 중 본문이 없는 작업은 아래 기록으로 구현 내용과 판단을 보완한다.

## `b754c95 chore(setup): React와 TypeScript 기반 프로젝트 초기 구성`

- React, Vite, TypeScript, TanStack Query, CSS Modules, Vitest 환경 구성
- Node.js 20.10 호환성을 고려해 Vite와 테스트 관련 패키지 버전 조정

## `f382c4c feat(mock-api): 지원자 조회 및 단계 저장 API 구현`

- 자체 mock API에 200~800ms 지연과 약 15% 단계 변경 실패 적용
- 성공한 단계 변경을 localStorage에 저장하도록 API와 UI 책임 분리

## `40c08d4 feat(board-layout): 채용 단계별 보드 컬럼 구현`

- 5개 채용 단계 컬럼과 단계별 시각적 구분 구현
- `DESIGN.md`를 추가하고 색상, 간격, 모서리 스타일을 디자인 기준에 맞춤

## `06e651f feat(card-list): 단계별 지원자 카드 목록 구현`

- mock API 지원자를 현재 단계에 따라 컬럼별로 분류
- 카드에 이름, 직무, 지원일과 현재 단계 표시

## `12ce5c8 feat(stage-move): 액션 버튼 기반 카드 단계 이동 구현`

- 드래그 앤 드롭 대신 명시적인 단계 이동 버튼 선택
- 단계별 아이콘과 텍스트를 사용하고 mock API 저장 연결

## `643b9b0 feat(optimistic-update): 단계 이동 선반영 및 실패 롤백 구현`

- API 응답 전에 Query 캐시를 변경해 카드를 즉시 이동
- 실패 시 이전 전체 캐시 복원과 사용자 오류 안내 적용

## `5047a16 feat(search-filter): 지원자 검색 및 직무 필터 구현`

- 이름 검색과 직무 필터를 AND 조건으로 적용
- 필터 결과를 메모이제이션하고 현재 결과 인원 표시

## `7162cfc feat(detail-panel): 지원자 상세 사이드 패널 구현`

- 카드 선택 시 지원자 상세 정보를 사이드 패널로 표시
- 배경 클릭, 닫기 버튼, Escape 닫기와 배경 스크롤 잠금 적용

## `165f084 feat(loading-error-empty): 로딩 에러 및 빈 상태 구현`

- 최초 로딩, 조회 실패, 전체 데이터 없음과 필터 결과 없음 상태 분리
- 개발 환경의 `?mock=error` 주소로 조회 실패를 직접 재현

## `5d7f79c fix(loading-error-empty): 다시 시도 시 에러 상태 복구`

- 다시 시도 시 `mock=error` 쿼리를 제거한 뒤 목록 재조회
- 에러 재현 화면에서 기존 보드로 돌아가지 못하던 문제 수정

## `c042581 feat(a11y): 키보드 단계 이동과 상세 열람 지원`

- 키보드로 상세 보기와 단계 이동을 실행할 수 있도록 포커스 스타일 보완
- 상세 패널 포커스 제한, Escape 닫기와 기존 카드 포커스 복원

## `e21525d test(optimistic-update): 단계 이동 롤백 로직 검증`

- mutation hook의 API 응답 전 캐시 선반영을 직접 검증
- API 실패 시 mutation 전 전체 지원자 캐시 복원 검증

## `bb7dcaf fix(concurrency): 동일 카드 연속 이동 시 경쟁 상태 방지`

- 같은 지원자의 진행 중 요청에 추가 mutation이 발생하지 않도록 차단
- 다른 지원자의 단계 이동은 동시에 허용하도록 테스트

## `e8c5184 feat(undo): 최근 지원자 단계 이동 취소 구현`

- 최근 성공한 이동 1건과 이전 단계를 기록해 이동 취소 제공
- 취소 결과도 낙관적으로 반영하고 mock API에 저장

## `e33cdf2 feat(virtualization): 1,000건 지원자 가상 스크롤 구현`

- 컬럼별로 보이는 카드와 주변 카드만 렌더링
- 작은 화면에서 컬럼 너비 유지와 보드 가로 스크롤 적용
- 고정 높이 가상화 방식과 1,000건 필터 테스트 채택
