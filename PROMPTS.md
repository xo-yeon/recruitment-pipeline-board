# AI 협업 기록

## [setup] 프로젝트 환경 설정

### 프롬프트 1

React 채용 파이프라인 보드 구현을 위한 환경 설정

기술:
- React
- Vite
- TypeScript
- TanStack Query
- CSS Modules
- Vitest
- React Testing Library
- npm

요구사항:
1. 개발 서버, 빌드, lint, 테스트용 npm 스크립트 구성
2. TanStack Query Provider를 애플리케이션 진입점에 연결
3. 테스트 실행에 필요한 기본 설정 추가
4. 기능별 구현에 적합한 최소 폴더 구조 구성

기능 순서(참고용)
setup
→ mock-api
→ board-layout
→ card-list
→ stage-move
→ optimistic-update
→ search-filter
→ detail-panel
→ loading-error-empty
→ a11y-keyboard
→ optimistic-update-test
→ concurrency
→ documentation

### AI 출력 요지

- Vite와 TypeScript 설정 및 개발·빌드·lint·테스트 스크립트를 구성했다.
- 애플리케이션 진입점에 독립된 TanStack Query Provider를 연결했다.
- jsdom 기반 Vitest와 React Testing Library 설정 및 기본 렌더링 테스트를 추가했다.
- Provider, 전역 스타일, 테스트 설정만 분리한 최소 폴더 구조를 구성했다.

### 리뷰 / 검증

- 1차 출력 그대로 채택.

## [mock-api] 지원자 조회 및 단계 저장 API

### 프롬프트 1

지원자 mock API 구현

요구사항:
1. 지원자 타입과 250명의 시드 데이터 구성
2. 지원자 목록 조회와 단계 변경 API 구현
3. 요청에 200~800ms 네트워크 지연 적용
4. 단계 변경 요청에 약 15% 실패 적용
5. 성공한 변경 사항을 localStorage에 저장
6. UI가 저장소에 직접 접근하지 않도록 API 계층 분리

이번 작업에서는 보드 UI와 낙관적 업데이트를 제외한다.

### AI 출력 요지

- 지원자와 채용 단계 타입 및 250명의 시드 데이터를 구성했다.
- 네트워크 지연과 단계 변경 실패를 적용한 조회·수정 API를 구현했다.
- 변경 결과를 localStorage에 저장하고 저장 데이터 검증 로직을 추가했다.
- 조회, 저장, 실패 상황을 검증하는 테스트를 추가했다.

### 리뷰 / 검증

- npm run test:run 테스트 후 1차 출력 그대로 채택.

## [board-layout] 채용 단계별 보드 레이아웃

### 프롬프트 1

채용 단계별 보드 레이아웃 구현

요구사항:
1. 서류 검토, 면접, 처우 협의, 최종 합격, 불합격 컬럼 구성
2. 각 컬럼에 단계명, 설명, 지원자 수 영역 표시
3. 작은 화면에서도 사용할 수 있는 가로 스크롤 보드 구성
4. 단계 정보를 상수로 분리해 재사용 가능하게 구성

이번 작업에서는 지원자 카드와 mock API 연결을 제외한다.

### AI 출력 요지

- 채용 단계 정보를 재사용 가능한 상수로 분리했다.
- 보드와 컬럼 컴포넌트를 구성하고 5개 단계를 표시했다.
- 좁은 화면에서는 컬럼을 가로 스크롤할 수 있도록 구성했다.

### 리뷰 / 검증

- 테스트를 실행해 5개 채용 단계가 모두 표시되는 것을 확인했다.

### 프롬프트 2

디자인은 DESIGN.md를 참고해서 구현

### AI 출력 요지

- 흰 캔버스, 단일 파란색 강조, pill 형태, 24px 카드 반경을 반영했다.

### 리뷰 / 검증

- 문제: 디자인 스타일을 추가하고 싶음
- 판단: Coinbase 기반 `DESIGN.md` 파일 추가
- 수정: 기존 CSS 색상과 여백을 `DESIGN.md` 기준으로 변경

## [card-list] 단계별 지원자 카드 목록

### 프롬프트 1

단계별 지원자 카드 목록 구현

요구사항:
1. TanStack Query로 mock API의 지원자 목록 조회
2. 지원자를 현재 채용 단계의 컬럼에 분류
3. 카드에 이름, 직무, 지원일, 현재 단계 표시
4. 컬럼별 인원수와 전체 지원자 수 표시
5. DESIGN.md 기준의 카드 스타일 적용

이번 작업에서는 단계 이동과 상세 보기를 제외한다.

### AI 출력 요지

- 지원자 조회 Query hook과 단계별 분류 로직을 추가했다.
- 지원자 정보를 표시하는 카드 컴포넌트를 구현했다.
- 컬럼별 인원수와 전체 지원자 수를 조회 데이터에 연결했다.

### 프롬프트 2

각 채용 단계가 구분되도록 단계 진행에 따라 컬럼 배경색을 다르게 적용

### AI 출력 요지

- 진행 단계별로 배경 농도를 조정하고 최종 상태를 구분했다.


### 프롬프트 3

지원자 카드의 border-radius 축소

### AI 출력 요지

- 지원자 카드 모서리 반경을 24px에서 8px로 줄였다.

### 리뷰 / 검증

- 문제: 모든 컬럼의 배경이 같아 채용 단계 구분이 약함
- 수정: 컬럼의 단계 값에 따라 배경색과 테두리 색상을 변경

- 문제: 지원자 카드의 모서리가 컬럼과 동일해 계층 구분이 약함
- 수정: 지원자 카드의 border-radius를 8px로 변경

## [stage-move] 지원자 단계 이동

### 프롬프트 1

액션 버튼 기반 지원자 단계 이동 구현

요구사항:
1. 진행 중 단계에서 이전·다음 단계 이동 제공
2. 진행 중인 지원자를 바로 불합격 처리 가능
3. 최종합격과 불합격은 종료 상태로 처리
4. 단계 변경 시 mock API에 저장하고 목록 다시 조회
5. 요청 중에는 중복 이동 방지를 위해 버튼 비활성화

이번 작업에서는 낙관적 업데이트와 실패 롤백을 제외한다.

### AI 출력 요지

- 단계별 이동 가능한 액션을 상수로 분리했다.
- 카드에 단계 이동 버튼과 mutation hook을 연결했다.
- API 성공 후 지원자 목록을 다시 조회하도록 구성했다.

### 프롬프트 2

각 단계와 이동 버튼에 아이콘을 표시해 이동 대상을 간단하게 안내

### AI 출력 요지

- 5개 채용 단계별 아이콘을 추가했다.
- 이동 버튼에 대상 단계 아이콘을 표시했다.

### 리뷰 / 검증

- 문제: 텍스트만으로는 단계와 이동 대상 구분이 느림
- 판단: 아이콘과 텍스트를 함께 사용해 의미와 접근성을 유지
- 수정: 컬럼, 현재 단계 배지, 이동 버튼에 단계 아이콘을 추가

## [optimistic-update] 단계 이동 낙관적 업데이트

### 프롬프트 1

지원자 단계 이동 낙관적 업데이트 구현

요구사항:
1. 단계 이동 시 API 응답 전에 UI에 먼저 반영
2. mutation 전에 지원자 목록 캐시 저장
3. API 실패 시 이전 캐시로 원상 복구
4. 실패 내용을 사용자에게 안내
5. 요청 완료 후 서버 데이터 다시 조회

### AI 출력 요지

- mutation 전에 Query 캐시를 저장하고 이동 결과를 즉시 반영했다.
- 실패 시 저장한 캐시를 복원하고 오류 메시지를 표시했다.
- 성공 여부와 관계없이 요청 완료 후 데이터를 다시 조회했다.

### 프롬프트 2

버튼 비활성화와 로딩 커서 없이 UI를 즉시 반영하고, 약 15% 실패 시 원상 복구와 사용자 피드백 적용

### AI 출력 요지

- 요청 중 버튼 비활성화와 로딩 커서를 제거했다.
- API 실패 시 이전 상태 복구를 알리는 오류 메시지를 표시했다.

### 리뷰 / 검증

- 문제: 요청 중 버튼이 비활성화되고 커서가 로딩 상태로 변경되어 낙관적 업데이트가 즉시 반영되지 않는 것처럼 보임
- 확인: 웹에서 단계 이동 버튼을 직접 클릭해 버튼 상태와 카드 이동을 재현
- 판단: 네트워크 요청은 백그라운드에서 처리하고 UI는 즉시 반영되어야 함
- 수정: 버튼 비활성화 로직과 로딩 커서 스타일을 제거
