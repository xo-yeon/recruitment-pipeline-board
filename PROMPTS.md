# AI 협업 기록

## [setup] 프로젝트 환경 설정

### 프롬프트 1

React 채용 파이프라인 보드 구현을 위한 환경 설정

기술:
- React
- Vite
- TypeScript
- TanStack Query
- CSS Modules
- Vitest
- React Testing Library
- npm

요구사항:
1. 개발 서버, 빌드, lint, 테스트용 npm 스크립트 구성
2. TanStack Query Provider를 애플리케이션 진입점에 연결
3. 테스트 실행에 필요한 기본 설정 추가
4. 기능별 구현에 적합한 최소 폴더 구조 구성

기능 순서(참고용)
setup
→ mock-api
→ board-layout
→ card-list
→ stage-move
→ optimistic-update
→ search-filter
→ detail-panel
→ loading-error-empty
→ a11y-keyboard
→ optimistic-update-test
→ concurrency
→ documentation

### AI 출력 요지

- Vite와 TypeScript 설정 및 개발·빌드·lint·테스트 스크립트를 구성했다.
- 애플리케이션 진입점에 독립된 TanStack Query Provider를 연결했다.
- jsdom 기반 Vitest와 React Testing Library 설정 및 기본 렌더링 테스트를 추가했다.
- Provider, 전역 스타일, 테스트 설정만 분리한 최소 폴더 구조를 구성했다.

### 리뷰 / 검증

- 1차 출력 그대로 채택.

## [mock-api] 지원자 조회 및 단계 저장 API

### 프롬프트 1

지원자 mock API 구현

요구사항:
1. 지원자 타입과 250명의 시드 데이터 구성
2. 지원자 목록 조회와 단계 변경 API 구현
3. 요청에 200~800ms 네트워크 지연 적용
4. 단계 변경 요청에 약 15% 실패 적용
5. 성공한 변경 사항을 localStorage에 저장
6. UI가 저장소에 직접 접근하지 않도록 API 계층 분리

이번 작업에서는 보드 UI와 낙관적 업데이트를 제외한다.

### AI 출력 요지

- 지원자와 채용 단계 타입 및 250명의 시드 데이터를 구성했다.
- 네트워크 지연과 단계 변경 실패를 적용한 조회·수정 API를 구현했다.
- 변경 결과를 localStorage에 저장하고 저장 데이터 검증 로직을 추가했다.
- 조회, 저장, 실패 상황을 검증하는 테스트를 추가했다.

### 리뷰 / 검증

- npm run test:run 및 직접 재현 테스트로 1차 출력 그대로 채택.

## [board-layout] 채용 단계별 보드 레이아웃

### 프롬프트 1

채용 단계별 보드 레이아웃 구현

요구사항:
1. 서류 검토, 면접, 처우 협의, 최종 합격, 불합격 컬럼 구성
2. 각 컬럼에 단계명, 설명, 지원자 수 영역 표시
3. 작은 화면에서도 사용할 수 있는 가로 스크롤 보드 구성
4. 단계 정보를 상수로 분리해 재사용 가능하게 구성

이번 작업에서는 지원자 카드와 mock API 연결을 제외한다.

### AI 출력 요지

- 채용 단계 정보를 재사용 가능한 상수로 분리했다.
- 보드와 컬럼 컴포넌트를 구성하고 5개 단계를 표시했다.
- 좁은 화면에서는 컬럼을 가로 스크롤할 수 있도록 구성했다.

### 리뷰 / 검증

- 테스트를 실행해 5개 채용 단계가 모두 표시되는 것을 확인했다.

### 프롬프트 2

디자인은 DESIGN.md를 참고해서 구현

### AI 출력 요지

- 흰 캔버스, 단일 파란색 강조, pill 형태, 24px 카드 반경을 반영했다.

### 리뷰 / 검증

- 문제: 디자인 스타일을 추가하고 싶음
- 판단: Coinbase 기반 `DESIGN.md` 파일 추가
- 수정: 기존 CSS 색상과 여백을 `DESIGN.md` 기준으로 변경

## [card-list] 단계별 지원자 카드 목록

### 프롬프트 1

단계별 지원자 카드 목록 구현

요구사항:
1. TanStack Query로 mock API의 지원자 목록 조회
2. 지원자를 현재 채용 단계의 컬럼에 분류
3. 카드에 이름, 직무, 지원일, 현재 단계 표시
4. 컬럼별 인원수와 전체 지원자 수 표시
5. DESIGN.md 기준의 카드 스타일 적용

이번 작업에서는 단계 이동과 상세 보기를 제외한다.

### AI 출력 요지

- 지원자 조회 Query hook과 단계별 분류 로직을 추가했다.
- 지원자 정보를 표시하는 카드 컴포넌트를 구현했다.
- 컬럼별 인원수와 전체 지원자 수를 조회 데이터에 연결했다.

### 프롬프트 2

각 채용 단계가 구분되도록 단계 진행에 따라 컬럼 배경색을 다르게 적용

### AI 출력 요지

- 진행 단계별로 배경 농도를 조정하고 최종 상태를 구분했다.


### 프롬프트 3

지원자 카드의 border-radius 축소

### AI 출력 요지

- 지원자 카드 모서리 반경을 24px에서 8px로 줄였다.

### 리뷰 / 검증

- 문제: 모든 컬럼의 배경이 같아 채용 단계 구분이 약함
- 수정: 컬럼의 단계 값에 따라 배경색과 테두리 색상을 변경

- 문제: 지원자 카드의 모서리가 컬럼과 동일해 계층 구분이 약함
- 수정: 지원자 카드의 border-radius를 8px로 변경

## [stage-move] 지원자 단계 이동

### 프롬프트 1

액션 버튼 기반 지원자 단계 이동 구현

요구사항:
1. 진행 중 단계에서 이전·다음 단계 이동 제공
2. 진행 중인 지원자를 바로 불합격 처리 가능
3. 최종합격과 불합격은 종료 상태로 처리
4. 단계 변경 시 mock API에 저장하고 목록 다시 조회
5. 요청 중에는 중복 이동 방지를 위해 버튼 비활성화

이번 작업에서는 낙관적 업데이트와 실패 롤백을 제외한다.

### AI 출력 요지

- 단계별 이동 가능한 액션을 상수로 분리했다.
- 카드에 단계 이동 버튼과 mutation hook을 연결했다.
- API 성공 후 지원자 목록을 다시 조회하도록 구성했다.

### 프롬프트 2

각 단계와 이동 버튼에 아이콘을 표시해 이동 대상을 간단하게 안내

### AI 출력 요지

- 5개 채용 단계별 아이콘을 추가했다.
- 이동 버튼에 대상 단계 아이콘을 표시했다.

### 리뷰 / 검증

- 문제: 텍스트만으로는 단계와 이동 대상 구분이 느림
- 판단: 아이콘과 텍스트를 함께 사용해 의미와 접근성을 유지
- 수정: 컬럼, 현재 단계 배지, 이동 버튼에 단계 아이콘을 추가

## [optimistic-update] 단계 이동 낙관적 업데이트

### 프롬프트 1

지원자 단계 이동 낙관적 업데이트 구현

요구사항:
1. 단계 이동 시 API 응답 전에 UI에 먼저 반영
2. mutation 전에 지원자 목록 캐시 저장
3. API 실패 시 이전 캐시로 원상 복구
4. 실패 내용을 사용자에게 안내
5. 요청 완료 후 서버 데이터 다시 조회

### AI 출력 요지

- mutation 전에 Query 캐시를 저장하고 이동 결과를 즉시 반영했다.
- 실패 시 저장한 캐시를 복원하고 오류 메시지를 표시했다.
- 성공 여부와 관계없이 요청 완료 후 데이터를 다시 조회했다.

### 프롬프트 2

버튼 비활성화와 로딩 커서 없이 UI를 즉시 반영하고, 약 15% 실패 시 원상 복구와 사용자 피드백 적용

### AI 출력 요지

- 요청 중 버튼 비활성화와 로딩 커서를 제거했다.
- API 실패 시 이전 상태 복구를 알리는 오류 메시지를 표시했다.

### 리뷰 / 검증

- 문제: 요청 중 버튼이 비활성화되고 커서가 로딩 상태로 변경되어 낙관적 업데이트가 즉시 반영되지 않는 것처럼 보임
- 확인: 웹에서 단계 이동 버튼을 직접 클릭해 버튼 상태와 카드 이동을 재현
- 판단: 네트워크 요청은 백그라운드에서 처리하고 UI는 즉시 반영되어야 함
- 수정: 버튼 비활성화 로직과 로딩 커서 스타일을 제거

## [search-filter] 지원자 검색 및 직무 필터

### 프롬프트 1

지원자 이름 검색과 직무 필터 구현

요구사항:
1. 이름 일부로 지원자 검색
2. 직무별 지원자 필터
3. 이름 검색과 직무 필터를 함께 적용
4. 검색어 앞뒤 공백과 대소문자 차이 무시
5. 200건 이상에서도 지연 없이 동작하도록 필터 결과 메모이제이션
6. 검색 결과 인원수 표시

### AI 출력 요지

- 이름 검색과 직무 선택 필터를 추가했다.
- 검색과 필터를 AND 조건으로 적용하고 결과를 메모이제이션했다.
- 현재 검색 결과 인원수를 화면에 표시했다.

### 리뷰 / 검증

- npm run test:run 및 직접 재현 테스트로 1차 출력 그대로 채택.

## [detail-panel] 지원자 상세 보기

### 프롬프트 1

지원자 카드 선택 시 상세 사이드 패널 구현

요구사항:
1. 카드 클릭 시 지원자 상세 패널 열기
2. 이름, 현재 단계, 직무, 지원일, 경력, 이메일, 연락처, 소개 표시
3. 닫기 버튼, 배경 클릭, Escape 키로 패널 닫기
4. DESIGN.md 기준의 사이드 패널 스타일 적용

### 프롬프트 2

상세 패널이 열린 동안 배경 body 스크롤 방지

### AI 출력 요지

- 카드 전체를 선택할 수 있는 상세 보기 버튼을 추가했다.
- 지원자 정보를 표시하는 사이드 패널과 배경을 구현했다.
- 닫기 버튼, 배경 클릭, Escape 키 닫기를 지원했다.
- 패널이 열리면 body 스크롤을 잠그고 닫을 때 기존 상태를 복원했다.

### 리뷰 / 검증

- 문제: 상세 패널이 열린 상태에서도 배경 화면이 스크롤됨
- 확인: 웹에서 상세 패널을 열고 배경 스크롤을 직접 재현
- 판단: 상세 패널이 열린 동안 배경 스크롤을 잠그고 닫을 때 복원
- 수정: 패널 on/off에 맞춰 body overflow를 변경하고 복원

## [loading-error-empty] 로딩·에러·빈 상태

### 프롬프트 1

지원자 보드의 로딩, 에러, 빈 상태 구현

요구사항:
1. 최초 지원자 조회 중 로딩 상태 표시
2. 조회 실패 시 오류 안내와 다시 시도 버튼 표시
3. 전체 지원자 데이터가 없을 때 빈 상태 표시
4. 검색과 필터 결과가 없을 때 별도 빈 상태 표시
5. 각 상태를 화면에서 명확하게 구분

### 프롬프트 2

각각 로딩, 에러, 빈 상태를 확인할 수 있는 방법을 알려줘.

### 프롬프트 3

콘솔 코드를 실행하지 않고 아래 주소로 에러 화면을 확인할 수 있도록 변경

```text
http://localhost:5173/?mock=error
```

요구사항:
1. 개발 환경에서 URL에 `?mock=error`가 있으면 지원자 조회 실패 처리
2. 주소에서 `?mock=error`를 제거한 후 다시 시도하면 정상 조회
3. 제출용 프로덕션 환경에는 영향을 주지 않도록 적용

### 프롬프트 4

에러 화면에서 다시 시도 버튼을 누르면 `?mock=error`를 제거하고 기존 보드 화면으로 돌아가도록 수정

### AI 출력 요지

- 로딩, 에러, 빈 상태를 표시하는 공통 컴포넌트를 추가했다.
- 조회 오류 안내와 다시 시도 기능을 구현했다.
- 전체 데이터와 검색 결과가 없는 경우를 구분해 안내했다.
- 개발 환경에서 URL의 `?mock=error`로 조회 실패를 재현할 수 있도록 추가했다.
- 다시 시도 시 `mock=error` 쿼리를 제거한 뒤 지원자 목록을 다시 조회하도록 수정했다.

### 리뷰 / 검증

- 문제: 조회 API가 항상 성공해 에러 상태를 웹에서 직접 확인할 수 없음
- 확인: mock API의 지원자 조회 로직에 실패 조건이 없는 것을 확인
- 판단: 제출 기능에 영향을 주지 않고 주소만으로 확인할 수 있도록 개발 환경에서만 임시 실패를 재현
- 수정: URL에 `?mock=error`가 있으면 조회 오류를 발생시키도록 변경

- 문제: 에러 화면에서 다시 시도해도 URL에 `?mock=error`가 남아 조회가 계속 실패함
- 확인: 다시 시도 버튼을 눌러도 에러 화면에서 기존 보드로 돌아가지 않는 것을 직접 확인
- 판단: 다시 시도할 때 임시 실패 조건을 먼저 제거한 후 목록을 재조회해야 함
- 수정: 다시 시도 버튼 클릭 시 `mock=error` 쿼리를 제거하고 지원자 목록을 다시 조회하도록 변경

## [a11y-keyboard] 키보드 접근성

### 프롬프트 1

키보드만으로 지원자 단계 이동과 상세 정보 열람이 가능하도록 웹 접근성 구현

요구사항:
1. Tab 키로 카드 상세 보기와 단계 이동 버튼에 접근
2. Enter 또는 Space 키로 상세 보기와 단계 이동 실행
3. 상세 패널이 열리면 패널 내부로 포커스 이동
4. 상세 패널이 열린 동안 Tab 포커스를 패널 내부로 제한
5. Escape 키로 패널을 닫고 기존 카드로 포커스 복원
6. 키보드 포커스 위치를 화면에서 명확하게 표시

### AI 출력 요지

- 카드 상세 보기와 단계 이동 버튼의 기본 키보드 동작을 유지했다.
- 상세 패널이 열리면 닫기 버튼으로 포커스를 이동하고 Tab 포커스를 패널 내부로 제한했다.
- Escape 또는 닫기 버튼으로 패널을 닫으면 기존 상세 보기 버튼으로 포커스를 복원했다.
- 상세 보기, 단계 이동, 패널 닫기 버튼에 키보드 포커스 스타일을 적용했다.

### 리뷰 / 검증

- npm run test:run 및 키보드 동작 테스트 후 1차 출력 그대로 채택.

## [optimistic-update-test] 낙관적 업데이트 핵심 테스트

### 프롬프트 1

지원자 단계 이동의 낙관적 업데이트와 실패 롤백 핵심 로직 테스트 추가

요구사항:
1. API 응답 전에 지원자 단계가 Query 캐시에 반영되는지 검증
2. 단계 이동 API가 호출되는지 검증
3. API 실패 시 mutation 전 전체 지원자 캐시로 복원되는지 검증
4. 화면 동작과 분리해 mutation hook의 캐시 변경을 직접 검증

### AI 출력 요지

- 단계 이동 mutation hook을 독립적으로 실행하는 테스트 환경을 구성했다.
- API 응답 전 Query 캐시의 지원자 단계가 변경되는지 검증했다.
- API 실패 시 mutation 전에 저장한 전체 지원자 캐시가 복원되는지 검증했다.

### 리뷰 / 검증

- npm run test:run 테스트 후 1차 출력 그대로 채택.

## [concurrency] 동일 카드 연속 이동 경쟁 상태 처리

### 프롬프트 1

같은 지원자 카드를 빠르게 연속 이동해도 데이터가 깨지지 않도록 경쟁 상태 처리

요구사항:
1. 같은 지원자의 단계 변경 요청이 진행 중이면 추가 이동 요청 방지
2. 다른 지원자의 단계 이동은 계속 허용
3. 버튼 비활성화와 로딩 커서 없이 기존 낙관적 업데이트 유지
4. 동일 지원자 중복 요청 방지와 다른 지원자 요청 허용을 테스트

### AI 출력 요지

- 요청 중인 지원자 ID를 mutation hook 내부에서 관리했다.
- 같은 지원자의 추가 이동 요청은 API 호출과 캐시 변경 전에 차단했다.
- 다른 지원자의 단계 이동은 독립적으로 실행되도록 유지했다.
- 버튼 상태와 커서를 변경하지 않고 중복 요청 방지 테스트를 추가했다.

### 리뷰 / 검증

- npm run test:run으로 동일 지원자 중복 요청 방지와 다른 지원자 이동 허용 테스트가 통과하는 것을 확인한 후 1차 출력 그대로 채택.
